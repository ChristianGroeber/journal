version: "3"

services:
    '2022':
        build:
            context: .
            dockerfile: docker/local.Dockerfile
            args:
                UID: 33
                GID: 33
        container_name: '2022'
        ports:
            - '86:80'
        volumes: 
            - ./:/var/www/html
        environment:
            USER_FILE: 'users/users.json'
            SECRET: 'test'