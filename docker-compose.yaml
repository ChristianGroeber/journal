version: "3"

services:
    journal:
        build:
            context: .
            dockerfile: docker/local.Dockerfile
            args:
                UID: 33
                GID: 33
        container_name: journal
        ports:
            - '86:80'
        volumes: 
            - ./:/var/www/html