version: "3"

services:
    journal:
        build:
            context: .
            dockerfile: docker/local.Dockerfile
            args:
                UID: 1000
                GID: 1000
        container_name: journal
        ports:
            - '94:80'
        volumes: 
            - ./:/var/www/html